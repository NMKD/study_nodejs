<h1>Корзина</h1>

<div id="card">
    {{#if courses.length}}
    <table class="striped">
        <thead>
            <tr>
                <th>Название</th>
                <th>Кол-во</th>
                <th>Цена</th>
                <th>Действия</th>
            </tr>
        </thead>
        <tbody>
            {{#each courses}}


            <tr>
                <td>{{title}}</td>
                <td>{{count}}</td>
                <td class="price total-table__price">{{price}}</td>
                <td>
                    <button class="waves-effect waves-light btn-small js-remove" data-id="{{id}}">Удалить</button>
                </td>
            </tr>


            {{/each}}
        </tbody>
    </table>
    <div class="total">
        <div>
            <strong>
                Итого:
            </strong>
        </div>
        <div class="price total-table__price">{{priceJson}}</div>
    </div>
    {{else}}
    <p>Корзина пуста</p>
    {{/if}}
</div>